# Простой пример проекта на CMake
- `src` - папка с исходным кодом (заголовочные и исполняемые файлы). Собирается как статическая библиотека.
- `tests` - папка с тестами. Используется фреймворк `gtest`
